name: Deploy Redis

on:
  push:
    branches:
      - ci-cd
    paths:
      - 'apps/backend/docker-compose.redis.yaml'
      - '.github/workflows/deploy-redis.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      SERVER: 3.36.49.219
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH key and known hosts
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H $SERVER >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Copy docker-compose file
        run: |
          scp -i ~/.ssh/id_rsa apps/backend/docker-compose.redis.yaml ubuntu@$SERVER:/home/ubuntu/

      - name: Deploy Redis stack
        run: |
          ssh -i ~/.ssh/id_rsa ubuntu@$SERVER "cd /home/ubuntu && sudo docker compose -f docker-compose.redis.yaml pull && sudo docker compose -f docker-compose.redis.yaml up -d --force-recreate"

      - name: Verify deployment
        run: |
          sleep 10
          ssh -i ~/.ssh/id_rsa ubuntu@$SERVER "sudo docker ps --filter 'name=redis' --filter 'name=redis-exporter' --format 'table {{.Names}}\t{{.Status}}'"

