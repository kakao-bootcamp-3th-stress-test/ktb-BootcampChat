version: '3.8'

services:
  redis:
    image: redis:7.2
    container_name: redis-ktb
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - redis-network

networks:
  redis-network:
    driver: bridge

volumes:
  redis_data:

